{lib_dirs, [
    "deps"
]}.

{erl_opts, [
    warnings_as_errors
]}.

{edoc_opts, [
    {sort_functions, false},
    {preprocess, true},
    {todo, true}
]}.

{cover_enabled, true}.

{xref_checks, [
    undefined_function_calls
]}.

% vim:ft=erlang
